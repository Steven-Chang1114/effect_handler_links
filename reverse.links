fun run() {
    do ReversePrint("Hello\n");
    do ReversePrint("World\n")
}

fun printMessage(m) {
    handle(m()) {
        case ReversePrint(val, k) -> k(()); print(val)
        case Return(x) -> ()
    }
}

printMessage(run)